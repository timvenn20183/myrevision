<% button_text = 'Continue' if questions_answered > 0 %>
<% button_text = 'Start' if questions_answered == 0 %>
<%= link_to button_text, {:action => 'revise', :controller => 'courses', :id => encrypt(course.id), :remote => true} , {:class => 'button small radius left'} %>
